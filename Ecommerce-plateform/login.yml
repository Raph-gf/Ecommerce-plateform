info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/auth/login
  body:
    type: json
    data: |-
      {
          "email": "emelyne@ecom.com",
          "password": "Password1!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
