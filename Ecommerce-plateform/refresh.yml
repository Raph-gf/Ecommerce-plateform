info:
  name: refresh
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
